PORT=5000

# MySQL connection (get these from Railway's MySQL plugin -> Connect tab)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

JWT_SECRET=

# Comma-separated list of allowed frontend origins
FRONTEND_URL=http://localhost:3000
